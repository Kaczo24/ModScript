let a = 0;
let c = 0;
let b = 0;
while(true)
{
	a = InputN();
	c = Input();
	b = InputN();
	if (c == "+")
		Printl(a + b);
	else if (c == "+")
		Printl(a + b);
	else if (c == "-")
		Printl(a - b);
 	else if (c == "*")
		Printl(a * b);
	else 
		Printl(a / b);
	Print("\n");
}